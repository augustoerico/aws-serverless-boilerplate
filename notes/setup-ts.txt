requires: NodeJS + NPM

1-  Install `typescript` as dev-dep
```shell
npm install typescript --save-dev
```

2- Generate `tsconfig.json`
```shell
tsc --init
```

3- Update `tsconfig.json` with:
```json
{ ..., "baseUrl": "./src", ... }
```
